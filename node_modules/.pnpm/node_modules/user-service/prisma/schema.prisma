generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL") // Sẽ lấy từ Docker
}

// Model mẫu
model User {
  id    String @id @default(uuid())
  email String @unique
  name  String?
}